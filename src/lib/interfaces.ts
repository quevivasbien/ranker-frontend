export interface Item {
    name: string;
    description: string;
}

export interface User {
    name: string;
}

export interface UserRating {
    itemName: string;
    userName: string;
    rating: number;
}